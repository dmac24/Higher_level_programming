#!/usr/bin/python3
""" Exact Same Obeject """


def is_same_class(obj, a_class):
    """
    Return True if the object is exactly an instance
    of the specified class ; otherwise FalseueInherits from list.
    """
    return (type(obj) == a_class)
